# 9hwh 官网大改版路线图

## 1. 总原则

- 必须按阶段执行，不允许跨阶段乱改。
- 不允许先改首页再补规范。
- 不允许先处理旧 service 页面再做盘点。
- 不允许先做索引规则再确定页面分流。
- 每阶段完成后必须验收。

## 阶段 0：项目规范初始化

- 目标：建立治理基础，明确能做和不能做，防止继续无序生成页面或改动。
- 输入：站点问题描述、旧 service 页面情况、当前关键词池、长期官网目标。
- 输出：AGENTS.md 和根目录下全部规范文件。
- 禁止：不改首页代码，不处理 service 页面，不生成 sitemap、robots、noindex、301、410。
- 验收：规范文件齐全，阶段边界清晰，支持未来关键词和业务变化。

## 阶段 1：现状盘点

- 目标：建立真实页面资产清单，识别收录、流量、风险、质量和外链情况。
- 输入：全站 URL、搜索表现数据、访问日志、当前 sitemap/robots、页面内容样本。
- 输出：URL 总表、service 清单、收录点击清单、风险表达清单、重复度清单、初步分流建议。
- 禁止：不直接删除、noindex、301 或改 robots。
- 验收：所有可发现 URL 有基础标签，高风险表达有统计，可进入定位设计。

## 阶段 2：官网定位与信息架构

- 目标：确定长期官网定位，设计稳定主结构，明确核心服务边界。
- 输入：阶段 1 盘点结果、site-positioning.md、keyword-policy.md、content-policy.md。
- 输出：新站信息架构、首页模块结构、核心服务页清单、内容中心栏目、不承接范围。
- 禁止：不把当前关键词池做成导航，不把 service 页面当主结构，不用高风险词定义服务。
- 验收：主结构稳定，关键词层可替换，页面层级清晰。

## 阶段 3：新首页与核心页面设计

- 目标：用正式官网形态替代链接池首页，建立可信、克制、可维护的核心页面。
- 输入：信息架构、服务边界、内容政策、风险词规则。
- 输出：首页文案方案、核心服务页文案方案、联系入口方案、内链结构方案。
- 禁止：不生成 service_xxxx 链接池，不承诺包过审、不封号，不堆当前关键词池。
- 验收：首页像正式官网，核心页面无高风险表达，服务范围和不承接范围清楚。

## 阶段 4：关键词池治理体系

- 目标：让关键词池可替换、可审核、可记录，防止污染主结构。
- 输入：当前和未来关键词池、风险词库、内容中心栏目。
- 输出：关键词分级表、禁用词表、内容承接类型表、发布前审核表。
- 禁止：不自动发布关键词页面，不让高风险词进主结构，不用模板批量铺页面。
- 验收：每个关键词有分类，每类关键词有处理规则，可支持后续替换。

## 阶段 5：旧 service 页面分流策略

- 目标：按数据和规则处理旧页面，降低低质页面和风险表达拖累。
- 输入：URL 清单、收录点击数据、外链转化数据、风险表达清单、新承接页清单。
- 输出：保留、重写、合并、noindex、410、301 列表和分批执行计划。
- 禁止：不全删，不全 301 到首页，不直接 robots 屏蔽，不一次性大批量不可逆处理。
- 验收：每个 URL 有处理原因，每个 301 有相关承接页，每个 410 有删除依据。

## 阶段 6：索引治理策略

- 目标：让搜索引擎逐步理解新结构，控制低质页面继续扩散。
- 输入：旧页面分流结果、新页面清单、sitemap/robots/canonical 规划。
- 输出：sitemap、noindex、301、410、canonical 的规则方案和观察指标表。
- 禁止：不把所有旧页放进 sitemap，不用 robots 代替 noindex，不把无关页面 301 到首页。
- 验收：sitemap 只含规范可索引页面，noindex 页面可被抓取，跳转相关性明确。

## 阶段 7：内容中心与长期更新机制

- 目标：建立可长期更新的内容资产，用内容中心承接变化关键词。
- 输入：关键词治理结果、内容政策、用户问题清单、国家/平台/行业分类。
- 输出：内容中心栏目、选题池、文章模板边界、FAQ 规则、更新节奏、人工审核清单。
- 禁止：不纯模板批量生成核心内容，不堆关键词，不发布无审核高风险文章。
- 验收：每篇内容有明确用户问题和合规边界，关键词变化不会破坏主结构。

## 阶段 8：上线检查与观察机制

- 目标：降低上线风险，观察搜索引擎和用户行为变化，决定下一轮迭代。
- 输入：新站页面、旧页分流执行结果、索引治理执行结果、变更记录。
- 输出：上线检查表、观察指标表、异常处理规则、回滚点记录、下一轮迭代计划。
- 禁止：不上线未审核高风险表达，不在观察期频繁大改，不用单日数据判断成败。
- 验收：关键页面可访问，状态码符合预期，内链无明显错误，异常有处理路径。
