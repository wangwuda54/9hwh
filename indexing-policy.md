# 9hwh 索引治理规则

## 1. 索引治理目标

索引治理的目标是让搜索引擎逐步理解：哪些页面是长期主结构，哪些页面是可索引内容资产，哪些旧页面应降级、合并或移除，哪些页面不应继续参与搜索结果。

索引治理不是快速清空旧页面，而是降低混乱信号。

## 2. 基本原则

- 实际 sitemap、robots、noindex、301、410 规则的生成和执行，由 `project-status.md` 与 `stage-gates.md` 控制。
- sitemap 用来提示重要规范页面。
- robots 用来控制抓取范围，不替代 noindex。
- noindex 用来让可抓取页面退出索引。
- 301 用来处理相关页面迁移或合并。
- 410 用来处理确定永久移除的页面。
- canonical 用来处理重复或近似重复内容的规范化。
- 每个 URL 只能有一个主要治理意图。

## 3. sitemap 使用原则

sitemap 只放：首页、核心服务页、重要内容中心页面、重要国家/平台/行业指南、已审核可索引的规范页面。

sitemap 不放：未审核旧 service 页面、noindex 页面、410 页面、重定向页面、重复页面、低质模板页、临时测试页。

sitemap 是规范页面提示，不是收录保证。

## 4. robots 使用原则

robots 可以用于：屏蔽无搜索价值的后台路径、屏蔽参数型爬虫陷阱、降低不必要抓取消耗。

robots 不用于：移除已收录页面、替代 noindex、批量隐藏旧 service 页面、处理高风险内容。

如果页面需要 noindex，必须让搜索引擎能抓取到 noindex。

## 5. noindex 使用原则

noindex 用于：低质但暂时保留页面、需要退出搜索结果的旧页面、用户仍可访问但不希望被索引的页面、后续可能重写或合并的页面。

noindex 前必须确认：页面没有被 robots 屏蔽，页面能正常返回 200，noindex 标记在页面或响应头中可见，页面不在 sitemap 中。

## 6. 301 使用原则

301 用于：URL 结构迁移、多个旧页合并到高相关新页、旧内容被新内容完整替代。

301 不用于：无关页面跳首页、高风险页面甩给核心页、没有承接内容的兜底跳转、批量掩盖低质页面。

301 必须保证用户意图一致或高度接近。

## 7. 410 使用原则

410 用于永久删除、无保留价值、无搜索价值、无承接页、不应继续公开的页面。

410 前必须确认：无有效点击、无重要外链、无转化价值、无重写价值，并已记录回滚点。

410 必须分批执行。

## 8. canonical 使用原则

canonical 用于重复内容规范化、参数 URL 规范化、近似重复页面指向主版本、同主题多个版本明确主页面。

canonical 不用于替代 noindex、301、删除，也不用于处理完全不相关页面。

canonical 目标页必须可索引、内容完整、主题相关。

## 9. 治理顺序

正确顺序：

1. 冻结旧页面新增。
2. 盘点全部 URL。
3. 标记页面风险和价值。
4. 建立新主结构和承接页。
5. 从 sitemap 中移除不应索引的页面。
6. 对低价值保留页分批 noindex。
7. 对明确合并页分批 301。
8. 对永久废弃页分批 410。
9. 用 canonical 规范重复和参数页面。
10. 观察数据，再扩大批次。

## 10. 错误顺序

禁止：先 robots 屏蔽再 noindex；先全站 301 再建承接页；先全删再盘点；先提交 sitemap 再审核页面。

## 11. 避免搜索引擎误判

必须避免：

- 大量无关 301 到首页。
- sitemap 中混入 noindex 页面。
- robots 屏蔽需要 noindex 的页面。
- 页面内容和标题不一致。
- 首页继续作为 service 链接池。
- 新旧结构同时释放混乱信号。
- 频繁改状态码。

## 12. 每次治理后的观察指标

每批索引治理后观察：被索引页面数量、未编入索引原因、抓取频率、404/410/重定向异常数量、soft 404 数量、重复网页数量、Google 选择的 canonical 是否正确、自然搜索展示和点击、核心页面排名变化、旧 service 页面残留情况。

