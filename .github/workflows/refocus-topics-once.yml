name: Refocus 9HWH topics once

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - .github/workflows/refocus-topics-once.yml

permissions:
  contents: write

jobs:
  refocus-topics:
    if: ${{ !contains(github.event.head_commit.message, 'Apply refocused topic copy') }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Refocus topic copy
        shell: bash
        run: |
          set -euo pipefail
          python scripts/refocus_topics_copy.py

      - name: Build site
        shell: bash
        run: |
          set -euo pipefail
          python scripts/build_site.py

      - name: Check static site
        shell: bash
        run: |
          set -euo pipefail
          python scripts/check_static_site.py

      - name: Check sitemap readiness
        shell: bash
        run: |
          set -euo pipefail
          python scripts/check_sitemap_readiness.py

      - name: Commit generated changes
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add site_src/data/topics.json site/public data/content-assets docs || true
          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi
          git commit -m "Apply refocused topic copy"
          git push origin main
