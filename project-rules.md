# 9hwh 官网拯救项目硬规则

## 1. 项目性质

- 本项目是长期官网重建项目，不是短期收录项目。
- 本项目不是批量关键词页面项目，也不是旧 service 页面补丁项目。
- 所有执行必须服务于长期可信、稳定运营、可持续内容治理。

## 2. 可以做什么

允许做：

- 建立项目规范，盘点现有页面、关键词、收录、流量、风险表达。
- 设计官网长期定位、稳定信息架构、首页和核心服务页方案。
- 建立关键词池治理流程、旧 service 页面分流标准、索引治理顺序。
- 建立内容中心更新机制，分批执行低风险、可回滚改动。

## 3. 不能做什么

禁止做：

- 未盘点前批量删除、noindex、301 或 robots 屏蔽页面。
- 将旧 service 页面全部 301 到首页。
- 用 robots.txt 直接屏蔽旧 service 页面来替代 noindex。
- 把阶段性关键词写入首页、导航、核心服务页主结构。
- 继续批量生成模板化 service 页面。
- 用高风险承诺吸引点击。
- 在官网主结构中宣传规避审核、绕过风控、Cloak 等能力。
- 为了收录制造没有实质差异的大量页面。

## 4. 执行顺序

当前阶段以 `project-status.md` 为准。

必须按以下顺序执行：

1. 阶段 0：建立规范。
2. 阶段 1：盘点现状。
3. 阶段 2：确定定位与信息架构。
4. 阶段 3：设计新首页和核心页面。
5. 阶段 4：建立关键词池治理体系。
6. 阶段 5：旧 service 页面分流。
7. 阶段 6：索引治理。
8. 阶段 7：内容中心长期更新。
9. 阶段 8：上线检查和观察。

不得跨阶段乱改。

## 5. 变更规则

重大变化必须先记录，再执行；低风险小修可在阶段验收时汇总记录。

必须记录的变化包括：

- 业务方向、关键词池、服务范围、首页定位、核心页面结构变化。
- 旧页面处理策略、索引策略、风险词库、内容发布规则变化。

重大变更记录必须写入 change-log.md。

## 6. 业务方向变化处理

如果业务方向变化：

1. 先更新 site-positioning.md。
2. 再更新 keyword-policy.md。
3. 再检查 content-policy.md。
4. 再判断旧页面分流规则是否受影响。
5. 最后才允许改页面结构。

不得直接把新业务词塞进首页。

## 7. 关键词池变化处理

如果关键词池变化：

1. 记录关键词来源、日期、用途。
2. 按风险等级、搜索意图、业务匹配度分类。
3. 判断进入内容中心、FAQ、专题页，还是禁止使用。
4. 更新 change-log.md。

关键词池变化不等于官网定位变化。

## 8. 风险控制规则

高风险内容必须“三层隔离”：

- 不进入首页、导航、核心服务页。
- 不进入标题、描述、H1、主 CTA。
- 如必须讨论，只能用合规教育、风险说明、政策边界的方式表达。

不得把风险词包装成卖点。

## 9. 旧页面风险控制

旧 service 页面处理前必须有清单，字段至少包括：

- URL、状态码、是否收录、点击、展示、外链、转化。
- 是否含高风险表达、内容质量标签、推荐处理方式。

无清单，不处理。

## 10. 索引治理规则

- sitemap 只放希望被索引的规范页面。
- robots 只控制抓取，不作为移除索引的主要手段。
- noindex 页面必须允许搜索引擎抓取到该标记。
- 301 只用于有明确相关承接页的合并或迁移。
- 410 只用于确定永久删除且无保留价值的页面。
- canonical 只用于重复或近似重复页面的规范化，不替代删除。

## 11. 验收规则

每个阶段必须检查：

- 是否符合本文件规则。
- 是否更新相关规范。
- 是否引入高风险表达。
- 是否影响旧 service 页面。
- 是否影响索引状态。
- 是否保留回滚点。
- 是否记录变更。

未通过验收，不得进入下一阶段。

## 12. 个人团队执行原则

- 每次只做一个明确阶段。
- 每次只改一组文件或一类页面。
- 每次都保留修改记录。
- 索引治理小批量执行。
- 内容审核宁可慢，不可失控。
- 自动化只负责辅助，不替代决策。

## 13. 最终判断标准

一个改动是否应该做，看三点：

- 是否提升长期可信度。
- 是否降低历史风险。
- 是否支持未来变化。

不满足这三点，默认不做。
