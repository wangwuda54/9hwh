# 9hwh 官网拯救项目阶段门禁规则

## 1. 文件目的

本文件定义各阶段的准入条件、允许动作、禁止动作和退出条件。

当前项目状态以 `project-status.md` 为准。本文件用于判断某个动作是否可以在当前阶段执行。

## 2. 通用门禁原则

所有阶段都必须遵守：

- 不跨阶段执行高风险动作。
- 不在没有记录的情况下执行重大变更。
- 不在没有回滚方案的情况下执行索引、跳转、删除类动作。
- 不把旧 service 页面全部 301 到首页。
- 不用 robots.txt 屏蔽旧 service 页面来替代 noindex。
- 不继续批量生成模板化 service 页面。
- 不把高风险表达扩散到首页、导航、核心服务页、标题、描述、H1 和主 CTA。

## 3. 阶段 0：项目规范初始化

### 允许动作

- 新增和修订规范文件。
- 初始化独立 Git 仓库和重建期工作目录。
- 明确项目方向、阶段边界、索引原则、旧页面治理原则。
- 建立 project-status、stage-gates、inventory-schema 和阶段输入文档。

### 禁止动作

- 不改首页。
- 不改 sitemap。
- 不改 robots。
- 不改 `_headers`。
- 不改 `_redirects`。
- 不处理旧 service 页面。
- 不生成新页面。

### 退出条件

- 独立 Git 仓库已建立。
- 规范文件完整。
- 根目录规范文件已归档。
- 当前阶段限制已写入 project-status.md。
- 阶段门禁已写入 stage-gates.md。
- 旧页面盘点字段已写入 inventory-schema.md。
- 不再引用旧规范目录路径。
- 未改首页。
- 未改 sitemap。
- 未改 robots。
- 未处理旧 service 页面。
- 已建立阶段 1 首页重做输入清单。

### 进入阶段 1 前必须确认

- 已确认当前站点源码位置。
- 已确认首页文件。
- 已确认构建方式。
- 已确认 Cloudflare Pages 输出目录。
- 已确认 sitemap、robots、`_headers`、`_redirects` 当前状态。
- 已确认旧 service 链接池是否由脚本生成。

## 4. 阶段 1：现状盘点

### 允许动作

- 盘点仓库结构。
- 准备首页重做输入信息。
- 盘点首页、导航、页脚、sitemap、robots、`_headers`、`_redirects`。
- 盘点 service 页面生成方式。
- 导出 GSC 页面数据。
- 建立旧 service 页面 inventory 草表。

### 禁止动作

- 不批量处理旧 service 页面。
- 不批量改 sitemap、robots、noindex、301、410。
- 不重做首页上线。

### 退出条件

- 站点结构已盘点。
- 当前站点源码位置已确认。
- 当前首页文件已确认。
- 当前构建方式已确认。
- Cloudflare Pages 输出目录已确认。
- sitemap、robots、`_headers`、`_redirects` 当前状态已确认。
- service 页面来源已确认。
- 旧页面盘点字段和数据来源已确认。
- 首页重做所需文件位置已确认。

## 5. 阶段 2：官网定位与信息架构

### 允许动作

- 确认官网定位。
- 确认首页信息架构。
- 确认核心服务页、国家页、行业页、内容中心结构。
- 确认高风险表达替换规则。

### 禁止动作

- 不上线新首页。
- 不批量生成内容页。
- 不处理旧 service 页面。

### 退出条件

- 新官网主结构已确认。
- URL 结构已确认。
- 首页内容框架已确认。

## 6. 阶段 3：新首页与核心页面设计

### 允许动作

- 设计和开发首页。
- 设计和开发核心服务页。
- 调整导航和页脚到新主结构。
- 准备 Cloudflare Pages 预览验证。

### 禁止动作

- 不批量删除旧 service 页面。
- 不把旧 service 页面全部跳转到首页。
- 不直接释放未审核 sitemap。

### 退出条件

- 首页预览通过。
- 核心页面预览通过。
- 导航和页脚不再强化 service 链接池。
- 变更有回滚方案。

## 7. 阶段 4：关键词池治理体系

### 允许动作

- 建立关键词分级。
- 建立风险词库。
- 建立可进入内容中心的关键词清单。
- 建立禁止进入主结构的关键词清单。

### 禁止动作

- 不把阶段性关键词写死进首页、导航、核心服务页或 URL 主结构。
- 不用高风险关键词做主 CTA。

### 退出条件

- 关键词池已分级。
- 高风险词已隔离。
- 可发布范围已明确。

## 8. 阶段 5：旧 service 页面分流

### 允许动作

阶段 5 后，只有在完成以下前置条件后，才允许批量处理旧 service 页面：

- 已完成旧 service 页面盘点。
- 已按 inventory-schema.md 建立字段。
- 已完成保留、重写、合并、noindex、410、301 分流。
- 已记录每个 URL 的处理原因。
- 已记录证据来源。
- 已制定批次计划。
- 已制定回滚方案。
- 已明确观察指标。

### 禁止动作

- 不无清单处理。
- 不无证据处理。
- 不无回滚方案处理。
- 不把所有旧 service 页面 301 到首页。
- 不用 robots.txt 屏蔽旧 service 页面来替代 noindex。

### 退出条件

- 分流清单完成。
- 第一小批处理方案完成。
- 回滚方案完成。
- change-log.md 已记录批次计划。

## 9. 阶段 6：索引治理

### 允许动作

- 按已批准清单生成 sitemap、robots、noindex、301、410、canonical 相关配置。
- 分批执行 noindex、301、410。
- 观察 GSC 和抓取状态。

### 禁止动作

- 不把未审核页面放入 sitemap。
- 不把 noindex 页面放入 sitemap。
- 不对未分流旧页面批量执行索引治理。
- 不对需要 noindex 的页面使用 robots 屏蔽。

### 退出条件

- sitemap 只包含规范可索引页面。
- noindex、301、410 批次有记录。
- GSC 观察指标已建立。

## 10. 阶段 7：内容中心与长期更新机制

### 允许动作

- 建立内容中心。
- 发布少量高质量内容。
- 建立内容审核和更新机制。

### 禁止动作

- 不批量灌水。
- 不发布无实质差异页面。
- 不发布高风险承诺型内容。

### 退出条件

- 内容主题池已建立。
- 发布节奏已建立。
- 内容审核规则已建立。

## 11. 阶段 8：上线检查与观察

### 允许动作

- 执行上线前检查。
- 执行 Cloudflare Pages 预览验证。
- 提交 sitemap。
- 观察索引、抓取、点击、展示和异常。

### 禁止动作

- 不在未通过检查时合并上线。
- 不在异常未定位时继续扩大批次。

### 退出条件

- 上线检查通过。
- 回滚方案可执行。
- GSC 观察周期已建立。
